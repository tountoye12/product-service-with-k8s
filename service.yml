apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: spring-boot-deployment

spec:
  type: NodePort
  selector:
    app: product-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: "TCP"
      nodePort: 30001