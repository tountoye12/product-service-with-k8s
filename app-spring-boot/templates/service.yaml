apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-spring-boot.fullname" . }}
  labels:
    {{- include "app-spring-boot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-spring-boot.selectorLabels" . | nindent 4 }}
